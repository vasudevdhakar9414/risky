// SignupPage is removed. RiskyVasu is an invite-only platform.
// User accounts are created only by admins via the Admin Panel.
import React from 'react';
export const SignupPage: React.FC<{ setActiveTab: (tab: any) => void }> = () => null;
